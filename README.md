A simple time series prediction of Mackey Glass with MLP

run main.py

mackey_glass.py
    - Computes the values in each time step of mackey glass via eulers method
    - Handles the data formatting

mlp.py
    - MLP model
